function Navbar() {
  return (
    <nav>
      <div>
        <h2>Movie Explorer</h2>
      </div>

      <div>
        <a href="/">Home</a>
        <a href="/movies">Movies</a>
        <button>Explore Movies</button>
      </div>
    </nav>
  );
}

export default Navbar;
